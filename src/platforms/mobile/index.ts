import Camera from "../../components/camera/mobile/camera";
import CameraManager from "../../engine/camera_manager";

const camera = new Camera('mobile');
const camera_manager = new CameraManager(camera);

console.log('Hello Gallery_desctop');